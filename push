git add .
git commit -m "Pushing doko() - good for finding strings anywhere in client side (URL,HTML,Cookies,Storage,Indexed DB) checks include raw,urlenced,urldeced,base64 min substr lookups"
git push origin main
